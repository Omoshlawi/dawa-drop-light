import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Step2 = ({ courrierServices, onNext }) => {
  return (
    <View>
      <Text>Step2</Text>
    </View>
  );
};

export default Step2;

const styles = StyleSheet.create({});
