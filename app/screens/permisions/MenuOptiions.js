import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MenuOptiions = ({ navigation, route }) => {
  return (
    <View>
      <Text>MenuOptiions</Text>
    </View>
  );
};

export default MenuOptiions;

const styles = StyleSheet.create({});
