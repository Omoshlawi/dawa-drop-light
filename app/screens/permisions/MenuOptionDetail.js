import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MenuOptionDetail = ({navigation, route}) => {
  return (
    <View>
      <Text>MenuOptionDetail</Text>
    </View>
  );
};

export default MenuOptionDetail;

const styles = StyleSheet.create({});
