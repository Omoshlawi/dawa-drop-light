import { StyleSheet, Text, View } from "react-native";
import React from "react";

const DistributionEvents = () => {
  return (
    <View>
      <Text>DistributionEvents</Text>
    </View>
  );
};

export default DistributionEvents;

const styles = StyleSheet.create({});
