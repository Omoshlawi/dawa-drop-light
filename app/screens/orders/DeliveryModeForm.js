import { StyleSheet, Text, View } from "react-native";
import React from "react";

const DeliveryModeForm = ({ navigation }) => {
  return (
    <View>
      <Text>DeliveryModeForm</Text>
    </View>
  );
};

export default DeliveryModeForm;

const styles = StyleSheet.create({});
