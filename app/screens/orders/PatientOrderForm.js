import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PatientOrderForm = ({ navigation }) => {
  return (
    <View>
      <Text>PatientOrderForm</Text>
    </View>
  );
};

export default PatientOrderForm;

const styles = StyleSheet.create({});
